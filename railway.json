{
  "build": {
    "builder": "NIXPACKS",
    "config": {
      "phases": {
        "setup": "pip install --upgrade pip",
        "install": "pip install -r requirements.txt",
        "start": "gunicorn backend.app:app"
      }
    }
  }
}